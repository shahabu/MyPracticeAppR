<?xml version="1.0" encoding="utf-8"?>
<FrameLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context=".ui.home.MyorderFragment">

    <LinearLayout
        android:gravity="center"
        android:orientation="horizontal"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="20dp"
        android:layout_marginBottom="16dp">
        <Button
            android:textColor="@android:color/white"
            android:id="@+id/transferCarsButton"
            android:background="@drawable/rounded_accent_bg"
            android:layout_width="0dp"
            android:layout_height="40dp"
            android:text="@string/transfer_car"
            android:paddingStart="10dp"
            android:paddingEnd="10dp"
            android:layout_marginStart="10dp"
            android:layout_marginEnd="10dp"
            android:layout_weight="1"
            />
        <Button
            android:textColor="@color/white"
            android:id="@+id/nearestTruckButton"
            android:background="@drawable/rounded_accent_bg"
            android:layout_width="0dp"
            android:layout_height="40dp"
            android:layout_weight="1"
            android:text="@string/nearest_truck_text"
            android:layout_marginStart="10dp"
            android:layout_marginEnd="10dp"/>
    </LinearLayout>

    <TextView
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_gravity="center"
        android:text="You don't have any order ..."
        android:textSize="20sp"
        android:gravity="center"/>

</FrameLayout>